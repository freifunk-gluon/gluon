#!/bin/sh

cd "/lib/gluon/reload.d" || exit 1

err=0

for service in *; do
	echo "restarting: ${service} ..."
	exec "/etc/init.d/$service restart" || err=1
done

if [ $err -eq 1 ]; then
	echo 'One or more daemons failed to restart.'
	exit 1
fi

exit 0
