local info = fs.readfile('/proc/loadavg')
local loadavg = {}
local a, b, c
a, b, c = info:match('^([^ ]+) ([^ ]+) ([^ ]+)')

loadavg['1'] = tonumber(a)
loadavg['5'] = tonumber(b)
loadavg['15'] = tonumber(c)

return loadavg
